name: Alloy build and release

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: alloy-build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_linux_amd64:
    name: Build linux/amd64 (${{ matrix.profile }})
    if: github.event_name != 'push' || !startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        profile: [full, slim]
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Node.js
        if: matrix.profile == 'full'
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version-file: internal/web/ui/.nvmrc
          cache: npm
          cache-dependency-path: internal/web/ui/package-lock.json

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y make gcc xz-utils file

      - name: Build Alloy (${{ matrix.profile }})
        run: |
          if [ "${{ matrix.profile }}" = "slim" ]; then
            ./scripts/build-linux-amd64-slim.sh
          else
            ./scripts/build-linux-amd64.sh
          fi
        env:
          VERSION: ${{ github.sha }}

  release_linux_amd64:
    name: Release linux/amd64
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version-file: internal/web/ui/.nvmrc
          cache: npm
          cache-dependency-path: internal/web/ui/package-lock.json

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y make gcc xz-utils file

      - name: Build full profile
        run: ./scripts/build-linux-amd64.sh
        env:
          VERSION: ${{ github.ref_name }}

      - name: Package full binary
        run: |
          mkdir -p dist
          cp build/alloy dist/alloy-linux-amd64
          chmod +x dist/alloy-linux-amd64
          xz -9 -k dist/alloy-linux-amd64

      - name: Build slim profile
        run: ./scripts/build-linux-amd64-slim.sh
        env:
          VERSION: ${{ github.ref_name }}

      - name: Package slim binary
        run: |
          cp build/alloy dist/alloy-linux-amd64-slim
          chmod +x dist/alloy-linux-amd64-slim
          xz -9 -k dist/alloy-linux-amd64-slim
          ls -lh dist/

      - name: Publish GitHub release
        uses: softprops/action-gh-release@288696be3916479e99fa944c72825e9b3e192be4 # v2.0.3
        with:
          name: Alloy ${{ github.ref_name }}
          files: |
            dist/alloy-linux-amd64.xz
            dist/alloy-linux-amd64-slim.xz
          generate_release_notes: false
          body: |
            dontmint Alloy **${{ github.ref_name }}** — linux/amd64 release with **full** and **slim** build profiles.

            ## Assets

            | File | Profile | Compressed size (approx.) |
            | --- | --- | --- |
            | `alloy-linux-amd64.xz` | **full** | ~72 MB |
            | `alloy-linux-amd64-slim.xz` | **slim** | ~56 MB |

            ## Full vs slim — which binary should I use?

            Both builds share the same core metrics stack used for host monitoring and SQL metrics. Choose based on whether you need the full Alloy feature set or a smaller agent binary.

            ### Full (`alloy-linux-amd64.xz`)

            Use **full** when you need the complete Alloy distribution:

            - Embedded web UI (`embedalloyui`) for local config inspection and debugging
            - All upstream Flow components: Loki, Pyroscope, Beyla, Faro, database observability, and more
            - Kubernetes Prometheus operators (`prometheus.operator.*`)
            - Vendor Prometheus exporters (SNMP, Apache, cloud provider exporters, Redis, Kafka, etc.)
            - Full OpenTelemetry Collector module set

            Best for: exploratory configs, logging/tracing pipelines, Kubernetes operator workflows, or any config that references components outside the slim set.

            ### Slim (`alloy-linux-amd64-slim.xz`)

            Use **slim** when you only need metrics collection and remote write on Linux hosts:

            **Included**
            - `prometheus.exporter.unix`, `process`, `docker_state`, `smartctl`, `postgres`, `self`, `static`
            - `prometheus.scrape`, `prometheus.relabel`, `prometheus.remote_write`
            - `discovery.relabel`, `local.file`, `local.file_match`
            - `otelcol.receiver.sqlquery` → `otelcol.exporter.prometheus` (requires `--stability.level=public-preview`)

            **Excluded** (not linked into the binary)
            - Embedded web UI
            - Kubernetes Prometheus operators
            - Vendor Prometheus exporters beyond the list above
            - Loki, Pyroscope, Beyla, Faro, database observability
            - Most other OTel receivers/exporters

            Best for: fleet host agents, central SQL metrics collectors, and other Prometheus remote-write-only deployments where binary size matters.

            Build profile details: `docs/build-profiles.md`

            ## What's new

            ### `otelcol.receiver.sqlquery` (Public Preview)
            - OTel SQL Query Receiver for SQL metrics migration
            - ClickHouse (`clickhouse-go/v2`) and Postgres (`lib/pq`) drivers linked
            - Forked `internal/sqlquery` adds `driver = "clickhouse"` support (upstream v0.147.0 omits it)
            - Requires Alloy `--stability.level=public-preview`
            - Handoff: `docs/sqlqueryreceiver-build-handoff.md`

            ### `prometheus.exporter.smartctl`
            - SMART disk health metrics via smartctl JSON (Linux)

            ### `prometheus.exporter.docker_state`
            - Docker container state metrics from the local daemon

            ## Usage examples

            ```alloy
            otelcol.receiver.sqlquery "example" {
              driver              = "clickhouse"
              datasource          = "clickhouse://user:pass@host:9000/default"
              collection_interval = "60s"
              query {
                sql = "SELECT 1 AS value"
                metric {
                  metric_name  = "sql_example"
                  value_column = "value"
                  data_type    = "gauge"
                  value_type   = "double"
                }
              }
              output {
                metrics = [otelcol.exporter.prometheus.default.input]
              }
            }
            ```

            Unpack full: `xz -d alloy-linux-amd64.xz && chmod +x alloy-linux-amd64`

            Unpack slim: `xz -d alloy-linux-amd64-slim.xz && chmod +x alloy-linux-amd64-slim`
